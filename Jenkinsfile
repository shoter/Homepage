pipeline
{
    agent any
    stages
    {
        stage('Build')
        {
            steps
            {
                echo "Branch name: ${branch_name}"
                echo "${env.WORKSPACE}"
            }
        }

        stage('Test')
        {
            steps
            {

            }
        }
    }
}
