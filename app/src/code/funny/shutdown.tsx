import React, {Component} from "react";


export default class Shutdown extends Component {
    render() {
        return (<div className="shutdown-screen">
            It's now safe to turn off <br /> your tab.
        </div>)
    }
}